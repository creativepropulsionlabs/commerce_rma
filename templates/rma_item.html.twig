{#
/**
 * @file rma_item.html.twig
 * Default theme implementation to present RMA item data.
 *
 * This template is used when viewing RMA item pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_rma_item()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('rma_item') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
